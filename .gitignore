# 1. 의존성 (Dependencies) - 절대 올리면 안 됨 (용량 문제)
node_modules/
.pnp
.pnp.js

# 2. 환경 변수 및 보안 키 (Environment Variables) - ★ 가장 중요
# 카라반 앱의 지도 API 키, DB 비밀번호 등이 여기 들어갑니다.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 3. 빌드 결과물 (Build Outputs)
# 프론트엔드 빌드 폴더들
build/
dist/
.next/
out/

# 4. 디버그 로그 (Logs)
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs
*.log

# 5. 런타임 데이터 (Runtime data)
pids
*.pid
*.seed
*.pid.lock

# 6. 에디터 설정 파일 (Editor directories)
.vscode/
.idea/
*.swp
.DS_Store

# 7. (선택 사항) 로컬 테스트용 이미지 업로드 폴더
# 카라반 사진 테스트하다가 실수로 커밋하는 것 방지
uploads/
public/uploads/